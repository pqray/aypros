import { PlaceholderPage } from "@/components/shell/placeholder-page";

export default function LeadDetailPage() {
  return (
    <PlaceholderPage
      title="Detalhe do lead em construção"
      description="Notas, atividades e histórico do lead serão adicionados na fase do pipeline."
      actionHref="/pipeline"
      actionLabel="Voltar ao pipeline"
    />
  );
}
